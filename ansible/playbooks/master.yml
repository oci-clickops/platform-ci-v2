---
# Master Playbook
# Orchestrates all operations by importing operation-specific playbooks
# Usage: ansible-playbook master.yml --tags <operation_type>

- import_playbook: common/adb-lifecycle.yml
  tags: [adb-lifecycle]
